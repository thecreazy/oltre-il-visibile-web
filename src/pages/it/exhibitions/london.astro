---
import BaseLayout from '@layouts/BaseLayout.astro';
import ExhibitionDetailPage from '@components/ExhibitionDetailPage.astro';
import { getEntry } from 'astro:content';
import { pick } from '@utils/data';

const ex = (await getEntry('exhibitions', 'london'))!.data;
const title = `${pick(ex.city, 'it')} — ${ex.venue} · Oltre il Visibile`;
const desc = pick(ex.short_text, 'it');
---
<BaseLayout
  lang="it"
  title={title}
  description={desc}
  type="article"
  keywords={['mostra fotografica Londra','fine art nude Londra','Cromwell Place mostra']}
>
  <ExhibitionDetailPage lang="it" entryId="london" />
</BaseLayout>
