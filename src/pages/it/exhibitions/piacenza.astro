---
import BaseLayout from '@layouts/BaseLayout.astro';
import ExhibitionDetailPage from '@components/ExhibitionDetailPage.astro';
import { getEntry } from 'astro:content';
import { pick } from '@utils/data';

const ex = (await getEntry('exhibitions', 'piacenza'))!.data;
const title = `${pick(ex.city, 'it')} — ${ex.venue} · Oltre il Visibile`;
const desc = pick(ex.short_text, 'it');
---
<BaseLayout
  lang="it"
  title={title}
  description={desc}
  type="article"
  keywords={['mostra fotografica Piacenza','Palazzo Gotico mostra','fotografia e psicologia Piacenza']}
>
  <ExhibitionDetailPage lang="it" entryId="piacenza" />
</BaseLayout>
