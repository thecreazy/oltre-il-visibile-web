---
import BaseLayout from '@layouts/BaseLayout.astro';
import ExhibitionDetailPage from '@components/ExhibitionDetailPage.astro';
import { getEntry } from 'astro:content';
import { pick } from '@utils/data';

const ex = (await getEntry('exhibitions', 'piacenza'))!.data;
const title = `${pick(ex.city, 'en')} — ${ex.venue} · Oltre il Visibile`;
const desc = pick(ex.short_text, 'en');
---
<BaseLayout
  lang="en"
  title={title}
  description={desc}
  type="article"
  keywords={['photography exhibition Piacenza','Palazzo Gotico exhibition','photography and psychology Piacenza']}
>
  <ExhibitionDetailPage lang="en" entryId="piacenza" />
</BaseLayout>
