---
interface Props { href: string; class?: string; target?: string; rel?: string; }
const { href, class: className = '', target, rel } = Astro.props;
---
<a href={href} class={`alink ${className}`} target={target} rel={rel}>
  <slot />
</a>
